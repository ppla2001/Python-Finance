import numpy as np
arr = np.arange(0,10)
arr

arr + arr #adds elements on element by element position index 0 + index 0, index 1 + index 1, etc...
arr * arr #every number is squared
arr - arr #deberia dar todo 0
arr/arr #da warning en vez de parar el running del programa, da error porq no tiene sentido dividir 0/0
1/arr #da warning tmb

arr **3 #eleva todos los numeros al cubo
 
#mathematical operations
np.sqrt(arr) #square root
np.exp(arr) #e to the power of arr
np.sin(arr)
np.log(arr) 
#hay muchas, me puedo fijar en la documentation